function say(message = 'Hi') {
    console.log(message);
}

say(); // Hi
say('Hello') // Hello

///////////////////////////////////////////

function add(x, y) { // x, y are parameters
    return result = x + y;
}

add(100, 200); //100, 200 are arguments
console.log(result)